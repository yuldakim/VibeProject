# T05. DB 연결 실패 Fallback 처리 (Mock CSV)

## 목표
Supabase 연결/조회 실패 시 로컬 CSV로 대체하는 예외처리를 추가한다.

## 작업 내용
- data/mock_alarm_logs.csv 생성 (alarm_logs와 동일 컬럼, mock 2건 이상)
- get_alarm_logs()에 try/except 추가: 실패 시 CSV 로드 후 동일한 형태로 반환
- 로그로 fallback 발생 여부 표시 (print 또는 logging)

## 완료 조건
- [ ] SUPABASE_URL을 일부러 잘못 넣었을 때 CSV로 정상 fallback
- [ ] 반환 데이터 스키마가 정상 조회 케이스와 동일

## 참고
- 설계.md 3.1절 ("Fallback 예외 처리 포함")
- 의존: T04
