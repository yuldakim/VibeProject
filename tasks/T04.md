# T04. Supabase 연동 모듈 작성 (db.py)

## 목표
supabase-py로 alarm_logs를 조회하는 재사용 가능한 함수를 만든다.

## 작업 내용
- db.py: create_client(SUPABASE_URL, SUPABASE_KEY)
- get_alarm_logs() 함수: alarm_logs 전체/조건 조회, DataFrame 반환
- 단순 실행 스크립트로 조회 결과 확인 (print)

## 완료 조건
- [ ] db.py에서 mock 데이터 2건이 정상 조회됨
- [ ] 함수가 DataFrame(or list[dict])을 반환

## 참고
- 설계.md 3.1절
- 의존: T02, T03
