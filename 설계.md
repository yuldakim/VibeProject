# Project Prompt: Supabase & Vercel 기반 설비 이상감지 대시보드 MVP

## 1. 프로젝트 개요
- 목적: 설비 이상 발생 메세지 수신 시 복잡한 설비 시스템 접속 없이 즉시 파라미터 상태, 차트, 발생 이력 및 AI 분석 의견을 간편하게 조회하는 MVP 대시보드 구축[cite: 1]
- 핵심 기술 스택: Python (Streamlit/FastAPI), Supabase (PostgreSQL DB), Vercel (클라우드 배포), Plotly, Discord Webhook
- AI 개발 방법론: Claude Code 작성 코드 -> Gemini 적대적 교차 리뷰(AI Peer Review)를 통한 코드 안정성 및 완성도 극대화

---

## 2. Supabase DB 스키마 (alarm_logs)
Supabase SQL Editor에서 아래 쿼리를 실행하여 테이블을 생성하고 Mock 데이터를 Insert할 것.

CREATE TABLE alarm_logs (
    id SERIAL PRIMARY KEY,
    alarm_id VARCHAR(50),
    created_at TIMESTAMP DEFAULT NOW(),
    facility_name VARCHAR(100),
    parameter_name VARCHAR(100),
    current_value NUMERIC,
    threshold_value NUMERIC,
    alarm_type VARCHAR(50),
    ai_opinion TEXT
);

INSERT INTO alarm_logs (alarm_id, facility_name, parameter_name, current_value, threshold_value, alarm_type, ai_opinion)
VALUES 
('ALT-2026-001', '메인 쿨링 펌프 #1', '베어링 온도', 85.4, 75.0, '상한 초과', '최근 1시간 내 베어링 온도 급상승 패턴 감지. 임펠러 마모 또는 냉각수 순환 부전 가능성 85%. 현장 점검 권장.'),
('ALT-2026-002', '압출기 모터 #2', '토출 압력', 12.1, 15.0, '하한 미달', '토출 압력 지속 하강 패턴. 피더 막힘 또는 밸브 개폐 이상 확인 필요.');

---

## 3. 핵심 기능 요구사항

### 3.1 Supabase 연동 및 데이터 조회
- supabase-py 라이브러리를 사용하여 Supabase DB의 alarm_logs 테이블 데이터를 조회
- DB 연결 실패 시 로컬 Mock CSV 데이터를 읽어오는 Fallback 예외 처리 포함

### 3.2 디스코드 웹훅 알람 발송기 (discord_notifier.py)
- Supabase의 최신 이상 알람 정보를 Embed 형태의 카드 메시지로 디스코드 채널에 전송
- 카드 하단에 Vercel 배포 URL 링크 포함

### 3.3 대시보드 UI & AI 분석 리포트 (app.py)
- 상단 메트릭: Supabase에서 읽어온 오늘 발생 이상 건수 표기
- 이상 발생 리스트: Supabase alarm_logs 테이블 시각화
- 시계열 차트 & AI 진단 카드:
  - 선택한 파라미터의 시계열 Plotly 차트 (기준 임계치 수평 선 포함)
  - 차트 하단 ai_opinion 필드의 AI 분석 및 조치 가이드 카드로 시각적 출력

---

## 4. Vercel 배포 가이드
- vercel.json 및 requirements.txt 파일을 포함하여 Vercel에 원클릭 배포 가능하도록 구성
- Supabase URL과 Key는 .env 또는 Vercel Environment Variables로 관리

---

## 5. AI 적대적 코드 리뷰 가이드
1. 작성된 코드에 대해 Gemini에게 "이 코드에서 DB 커넥션 누수나 Vercel 배포 시 발생할 수 있는 에러 요소를 엄격하게 적대적으로 리뷰해줘" 요청 수행.
2. 지적된 리팩토링 사항을 반영하여 최종 코드 완성.